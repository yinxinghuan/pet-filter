import type { PetFilterCartridge } from './types';

// The asset generator overwrites this file only after every required cover and
// demo image exists. Null keeps the mother template on its canonical cartridge.
export const generatedCartridge: PetFilterCartridge | null = null;
